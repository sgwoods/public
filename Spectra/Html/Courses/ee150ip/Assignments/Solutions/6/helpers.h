/*
 * Prototypes for helper functions.
 */
#include <stdio.h>
#include <string.h>

int findCharacter(char s[], char c);
int countCharacter(char s[], char c);
void copySubString(char src[], char dest[], int startpos, int n);
int placeGroup(char start, char finish, char s[], int place);
int duplicates(char s[]);
int getline(char line[], int max);
