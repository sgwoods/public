Preserved artifacts, captures, and stable research material for the Inovia era
live here.

Current structure:

- `historic/artifacts/archive-html/` for preserved local HTML and XML captures

The initial seeded baseline was localized from the Steven archive layer so the
Inovia era archive can become restartable in its own right.
