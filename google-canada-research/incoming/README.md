Drop raw Google Canada finds here before classifying them.

Use the shared intake guidance in:

- `public/data/shared/incoming-artifact-analysis-playbook.md`
- `public/data/shared/incoming-artifact-analysis-template.md`

Recommended flow:

1. Place raw captures in `incoming/`.
2. Record them in `document-ingestion-log.md`.
3. Promote stable leads into `research/seed-leads.md`.
4. Move preserved items into `historic/` once classified.
